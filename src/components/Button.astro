---
/**
 * Button Component
 *
 * Reusable button with multiple variants
 *
 * Props:
 * @param variant - Button style variant (primary, secondary, outline)
 * @param size - Button size (sm, md, lg)
 * @param href - Optional link URL (renders as <a> instead of <button>)
 * @param type - Button type attribute (button, submit, reset)
 * @param class - Additional CSS classes
 */

interface Props {
  variant?: 'primary' | 'secondary' | 'outline';
  size?: 'sm' | 'md' | 'lg';
  href?: string;
  type?: 'button' | 'submit' | 'reset';
  class?: string;
}

const {
  variant = 'primary',
  size = 'md',
  href,
  type = 'button',
  class: className = '',
} = Astro.props;

const Tag = href ? 'a' : 'button';
const classes = `btn btn-${variant} btn-${size} ${className}`.trim();
---

<Tag
  class={classes}
  href={href}
  type={!href ? type : undefined}
  role={href ? 'button' : undefined}
>
  <slot />
</Tag>

<style>
  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-sm);
    padding: var(--space-md) var(--space-xl);
    font-family: var(--font-family-base);
    font-weight: 600;
    text-align: center;
    text-decoration: none;
    border: 2px solid transparent;
    border-radius: 10px;
    cursor: pointer;
    transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
    white-space: nowrap;
    letter-spacing: -0.01em;
  }

  /* Sizes */
  .btn-sm {
    font-size: var(--font-size-sm);
    padding: var(--space-sm) var(--space-lg);
  }

  .btn-md {
    font-size: var(--font-size-base);
    padding: var(--space-md) var(--space-xl);
  }

  .btn-lg {
    font-size: var(--font-size-md);
    padding: var(--space-lg) var(--space-2xl);
  }

  /* Primary Variant */
  .btn-primary {
    background-color: var(--color-primary);
    color: white;
    border-color: var(--color-primary);
    box-shadow: 0 4px 12px rgba(var(--color-primary-rgb), 0.15);
  }

  .btn-primary:hover {
    background-color: var(--color-primary-dark);
    border-color: var(--color-primary-dark);
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(var(--color-primary-rgb), 0.25), 0 2px 8px rgba(0, 0, 0, 0.1);
  }

  .btn-primary:active {
    transform: translateY(0);
    box-shadow: 0 4px 12px rgba(var(--color-primary-rgb), 0.15);
  }

  /* Secondary Variant */
  .btn-secondary {
    background-color: var(--color-secondary);
    color: white;
    border-color: var(--color-secondary);
    box-shadow: 0 4px 12px rgba(var(--color-secondary-light-rgb), 0.2);
  }

  .btn-secondary:hover {
    background-color: var(--color-secondary-dark);
    border-color: var(--color-secondary-dark);
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(var(--color-secondary-light-rgb), 0.3), 0 2px 8px rgba(0, 0, 0, 0.1);
  }

  .btn-secondary:active {
    transform: translateY(0);
    box-shadow: 0 4px 12px rgba(var(--color-secondary-light-rgb), 0.2);
  }

  /* Outline Variant */
  .btn-outline {
    background-color: transparent;
    color: var(--color-primary);
    border-color: var(--color-primary);
  }

  .btn-outline:hover {
    background-color: var(--color-primary);
    color: white;
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
  }

  .btn-outline:active {
    transform: translateY(0);
    box-shadow: var(--shadow-sm);
  }

  /* Outline variant on dark backgrounds (e.g., hero section) */
  :global(.hero-section) .btn-outline {
    background-color: transparent;
    color: var(--color-white);
    border-color: var(--color-white);
  }

  :global(.hero-section) .btn-outline:hover {
    background-color: var(--color-white);
    color: var(--color-primary);
    border-color: var(--color-white);
  }

  /* Focus state for accessibility */
  .btn:focus-visible {
    outline: 2px solid var(--color-secondary);
    outline-offset: 4px;
  }

  /* Disabled state */
  .btn:disabled,
  .btn[aria-disabled='true'] {
    opacity: 0.5;
    cursor: not-allowed;
    pointer-events: none;
  }
</style>
